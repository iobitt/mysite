h1 = @article.title

p = @article.body

p = link_to "Редактирование", edit_article_path(@article)

p = link_to "Удалить", article_path(@article), data: { turbo_method: :delete, turbo_confirm: "Вы уверены?" }

= link_to 'Назад', articles_path
